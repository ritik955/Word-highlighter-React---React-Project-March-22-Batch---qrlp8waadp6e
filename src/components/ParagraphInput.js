import React from 'react';

export const ParagraphInput = ({ pRef }) => {
    return (
        <>
            Enter paragraph here
            <div id="paragraph-input"
                contentEditable
            >
            </div>
        </>
    )
}